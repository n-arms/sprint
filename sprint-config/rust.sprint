import os, os.path, subprocess

def detect():
    if os.path.isfile(os.path.join(os.getcwd(), "Cargo.toml")):
        return 0

def run():
    result = subprocess.run(["cargo", "run"])
    return result.returncode
